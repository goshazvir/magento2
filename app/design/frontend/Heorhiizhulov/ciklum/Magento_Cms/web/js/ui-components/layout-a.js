define(['uiComponent'], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            label: 'Layout Component A',
            amount: 7
        }
    });
});
